extends /_layout/_default-layout

prepend config-block
  - pageName = 'International'

append config-block
  - pageId = 'publication-detail'
  - rootPath = '../../'
  - url = `${settings.url}publication/international/`

block content-block
  main
    .front-block
      +publication-list(bibArr, 'international')
